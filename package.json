{
  "name": "promise-flavor",
  "version": "1.1.0",
  "description": "Using promise with flavor. Control flow utilities for promise",
  "main": "index.js",
  "scripts": {
    "lint": "eslint .",
    "tape": "tape test/*.test.js",
    "coverage": "istanbul cover tape test/*.test.js",
    "coveralls": "nyc report --reporter=text-lcov | coveralls",
    "travis": "npm run lint && nyc npm run tape",
    "test": "npm run tape && npm run lint"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/allevo/promise-flavor.git"
  },
  "keywords": [
    "promise",
    "control-flow",
    "flow",
    "utilities"
  ],
  "author": "allevo",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "blue-tape": "^1.0.0",
    "coveralls": "^2.11.15",
    "eslint": "^3.12.2",
    "istanbul": "^0.4.5",
    "nyc": "^10.0.0",
    "tape": "^4.6.3"
  },
  "engines": {
    "node": ">=4"
  }
}
